import type { StyleDefinition } from '../types'
import { organicTokens } from './tokens'

export const organicStyle: StyleDefinition = {
  id: 'organic',
  name: 'Organic',
  description: 'Natural — vert sage, serif rond, photo botanique, vibe Aesop',
  tokens: organicTokens,
}

export { organicTokens }
