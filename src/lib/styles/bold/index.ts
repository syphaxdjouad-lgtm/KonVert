import type { StyleDefinition } from '../types'
import { boldTokens } from './tokens'

export const boldStyle: StyleDefinition = {
  id: 'bold',
  name: 'Bold',
  description: 'Bento layout + ombres hardlines + accent rose flash (Olipop vibe)',
  tokens: boldTokens,
}

export { boldTokens }
