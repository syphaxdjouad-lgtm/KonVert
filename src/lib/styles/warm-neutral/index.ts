import type { StyleDefinition } from '../types'
import { warmNeutralTokens } from './tokens'

export const warmNeutralStyle: StyleDefinition = {
  id: 'warm-neutral',
  name: 'Warm Neutral',
  description: 'Tan/beige sépia + serif éditorial (ALD/Norse Projects vibe)',
  tokens: warmNeutralTokens,
}

export { warmNeutralTokens }
