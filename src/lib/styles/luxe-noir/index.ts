import type { StyleDefinition } from '../types'
import { luxeNoirTokens } from './tokens'

export const luxeNoirStyle: StyleDefinition = {
  id: 'luxe-noir',
  name: 'Luxe Noir',
  description: 'Dark warm + or chaud — joaillerie/horlogerie premium',
  tokens: luxeNoirTokens,
}

export { luxeNoirTokens }
