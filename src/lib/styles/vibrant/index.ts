import type { StyleDefinition } from '../types'
import { vibrantTokens } from './tokens'

export const vibrantStyle: StyleDefinition = {
  id: 'vibrant',
  name: 'Vibrant',
  description: 'Couleurs vives + Clash Display + shadows colorées (Tonies/Notion vibe)',
  tokens: vibrantTokens,
}

export { vibrantTokens }
